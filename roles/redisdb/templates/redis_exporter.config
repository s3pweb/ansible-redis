{% for service in services -%}
redis://127.0.0.1:{{redis_port + services[service]['port_offset']}},{{secrets[service]['password']}},{{service}}
redis://127.0.0.1:{{sentinel_port + services[service]['port_offset']}},{{secrets[service]['password']}},sentinel-{{service}}
{% endfor %}
